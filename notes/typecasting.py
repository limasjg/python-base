# typecasting is the process pf converting a variable from one data type to another
# With native functions str(), int(), boo(), float()

name = "John"
age = 32
is_worker = True
height = 1.62

# If use type we can see the type of the variable
# print(type(height))

# To converting use the native function
age = float(age)

print(age)